# Getting Started with AI Learning System

## Setup
1. Ensure Python is installed.
2. Install the required libraries listed in `requirements.txt`.
3. Configure your environment using the `.env` file example.

## Usage Guide
- To start the chatbot, navigate to the `src` directory and run `python chatbot.py`.

## Configuration
- Update the `.env` file with your specific settings.